{
  "version": 2,
  "builds": [
    {
      "src": "app_2.py",
      "use": "@vercel/python",
      "config": { "maxDuration": 60 }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/app_2.py" }],
  "env": {
    "TESSDATA_PREFIX": "/app/.apt/usr/share/tessdata"
  }
}
